<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Super Cool App v2.0</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(45deg, #667eea, #764ba2);
            background-size: 400% 400%;
            animation: gradientShift 3s ease infinite;
            font-family: 'Comic Sans MS', cursive, sans-serif;
            overflow: hidden;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            position: relative;
            gap: 30px;
        }

        .message {
            font-size: 2.5rem;
            font-weight: bold;
            color: #fff;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.5);
            animation: wiggle 2s ease-in-out infinite;
            user-select: none;
            text-align: center;
            padding: 20px;
            border-radius: 20px;
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
        }

        @keyframes wiggle {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(1deg); }
            75% { transform: rotate(-1deg); }
        }

        .button-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            max-width: 700px;
        }

        .innocent-button {
            padding: 18px 30px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(0,0,0,0.2);
            color: white;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
            position: relative;
            overflow: hidden;
            font-family: 'Comic Sans MS', cursive;
        }

        .innocent-button:hover {
            transform: scale(1.05);
            box-shadow: 0 12px 30px rgba(0,0,0,0.3);
        }

        .innocent-button:active {
            transform: scale(0.98);
        }

        .btn-1 { background: linear-gradient(45deg, #ff9a9e, #fecfef); }
        .btn-2 { background: linear-gradient(45deg, #a8edea, #fed6e3); }
        .btn-3 { background: linear-gradient(45deg, #ffecd2, #fcb69f); }
        .btn-4 { background: linear-gradient(45deg, #ff8a80, #ff80ab); }
        .btn-5 { background: linear-gradient(45deg, #81c784, #aed581); }
        .btn-6 { background: linear-gradient(45deg, #64b3f4, #c2e59c); }
        .btn-7 { background: linear-gradient(45deg, #fbc2eb, #a6c1ee); }
        .btn-8 { background: linear-gradient(45deg, #fdbb2d, #22c1c3); }
        .btn-9 { background: linear-gradient(45deg, #ee9ca7, #ffdde1); }

        .memes {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .meme {
            position: absolute;
            font-size: 3rem;
            animation: memeFloat 4s ease-in-out infinite;
            opacity: 0.9;
        }

        @keyframes memeFloat {
            0% { 
                transform: translateY(100vh) rotate(0deg) scale(0.5);
                opacity: 0;
            }
            20% { opacity: 0.9; }
            80% { opacity: 0.9; }
            100% { 
                transform: translateY(-200px) rotate(360deg) scale(1.5);
                opacity: 0;
            }
        }

        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 0, 0, 0.9);
            color: white;
            padding: 10px 20px;
            border-radius: 10px;
            font-weight: bold;
            animation: slideIn 0.5s ease, slideOut 0.5s ease 2.5s forwards;
            z-index: 1000;
        }

        @keyframes slideIn {
            from { transform: translateX(100%); }
            to { transform: translateX(0); }
        }

        @keyframes slideOut {
            from { transform: translateX(0); }
            to { transform: translateX(100%); }
        }

        .sus-counter {
            position: absolute;
            top: 20px;
            left: 20px;
            color: white;
            font-size: 1.2rem;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
            background: rgba(0,0,0,0.3);
            padding: 10px 15px;
            border-radius: 15px;
        }

        @keyframes sus {
            0% { transform: scale(1); }
            50% { transform: scale(1.2) rotate(5deg); }
            100% { transform: scale(1); }
        }

        .airhorn {
            position: absolute;
            font-size: 4rem;
            animation: airhorn 0.5s ease-out forwards;
            pointer-events: none;
            z-index: 999;
        }

        @keyframes airhorn {
            0% { 
                transform: scale(0) rotate(0deg);
                opacity: 1;
            }
            50% {
                transform: scale(1.5) rotate(180deg);
                opacity: 1;
            }
            100% { 
                transform: scale(3) rotate(360deg);
                opacity: 0;
            }
        }

        .title {
            position: absolute;
            top: 50px;
            font-size: 2rem;
            color: white;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.7);
        }
    </style>
</head>
<body>
    <div class="title">🌟 Friendship Enhancement Tool 🌟</div>
    
    <div class="container">
        <div class="message" id="mainMessage">Welcome! Click buttons to enhance our friendship! 🥰</div>
        
        <div class="button-grid">
            <button class="innocent-button btn-1" onclick="enhanceFriendship(this, '💀 I HATE YOU! 💀', '🚨 GOTTEM 🚨')">🎁 Send Gift</button>
            <button class="innocent-button btn-2" onclick="enhanceFriendship(this, '😡 F YOU! 😡', '📢 BREAKING: STILL HATE U')">💝 Show Love</button>
            <button class="innocent-button btn-3" onclick="enhanceFriendship(this, '👹 I REALLY HATE YOU! 👹', '⚠️ PLOT TWIST ACTIVATED')">🤗 Big Hug</button>
            <button class="innocent-button btn-4" onclick="enhanceFriendship(this, '🔥 Did I mention I HATE YOU? 🔥', '🎪 CIRCUS MOMENT')">☀️ Brighten Day</button>
            <button class="innocent-button btn-5" onclick="enhanceFriendship(this, '🎭 Surprise! I HATE YOU! 🎭', '🤡 CLOWNED')">🎉 Party Mode</button>
            <button class="innocent-button btn-6" onclick="enhanceFriendship(this, '🎪 Plot twist: I HATE YOU! 🎪', '🎯 DIRECT HIT')">✨ Magic Spell</button>
            <button class="innocent-button btn-7" onclick="enhanceFriendship(this, '📺 Breaking news: I HATE YOU! 📺', '📡 TRANSMISSION SENT')">🎵 Play Song</button>
            <button class="innocent-button btn-8" onclick="enhanceFriendship(this, '🤓 Fun fact: I HATE YOU! 🤓', '🧠 BIG BRAIN MOVE')">🎲 Random Fun</button>
            <button class="innocent-button btn-9" onclick="enhanceFriendship(this, '💌 P.S. I HATE YOU! 💌', '📮 MESSAGE DELIVERED')">📱 Send Meme</button>
        </div>

        <div class="memes" id="memesContainer"></div>
        <div class="sus-counter" id="susCounter">Sus Level: 0% 📈</div>
    </div>

    <script>
        let susLevel = 0;
        const mainMessage = document.getElementById('mainMessage');
        const memesContainer = document.getElementById('memesContainer');
        const susCounter = document.getElementById('susCounter');

        const memeEmojis = ['😂', '💀', '🤡', '📸', '🎪', '🚨', '⚠️', '🔥', '💯', '🎭', '📢', '⭐'];
        const randomTrolls = [
            "🎪 WELCOME TO THE CIRCUS 🎪",
            "💀 GET REKT NOOB 💀",
            "🤡 YOU JUST GOT CLOWNED 🤡",
            "📸 CAUGHT IN 4K 📸",
            "🚨 BRUH MOMENT DETECTED 🚨",
            "🎭 IT'S JUST A PRANK BRO 🎭"
        ];

        // Create floating memes
        function createMeme() {
            const meme = document.createElement('div');
            meme.className = 'meme';
            meme.innerHTML = memeEmojis[Math.floor(Math.random() * memeEmojis.length)];
            meme.style.left = Math.random() * 100 + '%';
            meme.style.animationDelay = Math.random() * 2 + 's';
            meme.style.animationDuration = (Math.random() * 2 + 3) + 's';
            memesContainer.appendChild(meme);

            setTimeout(() => {
                meme.remove();
            }, 5000);
        }

        // Create memes periodically
        setInterval(createMeme, 1000);

        // Main troll function
        function enhanceFriendship(button, hateMessage, notification) {
            susLevel += Math.floor(Math.random() * 15) + 10;
            susCounter.textContent = `Sus Level: ${Math.min(susLevel, 100)}% 📈`;
            
            if (susLevel >= 100) {
                susCounter.textContent = "Sus Level: MAXIMUM SUS 🚨";
                susCounter.style.animation = 'sus 0.5s infinite';
            }
            
            // Update main message
            mainMessage.textContent = hateMessage;
            
            // Show meme notification
            const notif = document.createElement('div');
            notif.className = 'notification';
            notif.textContent = notification;
            document.body.appendChild(notif);
            
            setTimeout(() => {
                notif.remove();
            }, 3000);
            
            // Get button position for effects
            const rect = button.getBoundingClientRect();
            const centerX = rect.left + rect.width / 2;
            const centerY = rect.top + rect.height / 2;
            
            // Create airhorn effect
            const airhorn = document.createElement('div');
            airhorn.className = 'airhorn';
            airhorn.innerHTML = '📯';
            airhorn.style.left = centerX + 'px';
            airhorn.style.top = centerY + 'px';
            document.body.appendChild(airhorn);
            
            setTimeout(() => {
                airhorn.remove();
            }, 500);
            
            // Spawn extra memes on click
            for (let i = 0; i < 3; i++) {
                setTimeout(() => createMeme(), i * 200);
            }

            // Screen shake with meme effect
            document.body.style.transform = 'translate(' + (Math.random() * 10 - 5) + 'px, ' + (Math.random() * 10 - 5) + 'px) rotate(' + (Math.random() * 4 - 2) + 'deg)';
            setTimeout(() => {
                document.body.style.transform = 'translate(0, 0) rotate(0deg)';
            }, 200);

            // Button gets "sus"
            button.style.animation = 'sus 0.3s ease';
            setTimeout(() => {
                button.style.animation = '';
            }, 300);
        }

        // Auto troll messages
        setInterval(() => {
            if (Math.random() > 0.7) {
                const randomTroll = randomTrolls[Math.floor(Math.random() * randomTrolls.length)];
                mainMessage.textContent = randomTroll;
                
                // Spawn bonus meme
                createMeme();
            }
        }, 5000);

        // Random sus level increases
        setInterval(() => {
            if (Math.random() > 0.8) {
                susLevel += Math.floor(Math.random() * 5) + 1;
                susCounter.textContent = `Sus Level: ${Math.min(susLevel, 100)}% 📈`;
                
                if (susLevel >= 100) {
                    susCounter.textContent = "Sus Level: MAXIMUM SUS 🚨";
                }
            }
        }, 3000);
    </script>
</body>
</html>

